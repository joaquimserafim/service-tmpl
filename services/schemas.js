'use strict';

const Joi = require('joi');

const schemas = module.exports = {request, response};

schemas.request = Joi.object()
  .keys(
    {

    }
  );

schemas.response = Joi.object()
  .keys(
    {

    }
  );